#include <bits/stdc++.h>
using namespace std;

int main()
{
    int N;
    cin >> N;

    vector <int> cowsOrder (N);
    for (auto & c : cowsOrder) { cin >> c; }

    vector <int> doctorsOrder (N);
    for (auto & d : doctorsOrder) { cin >> d; }

    for (int i = 0; i <= N; i++)
    {
        for (int j = 0; j <= i; j++)
        {
            
        }
    }    

    return 0;
}