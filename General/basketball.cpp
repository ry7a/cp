#include <bits/stdc++.h>

using namespace std;

#define MAXN 210;

int main ()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    string record;

    cin >> record;
    cout << record[record.size()-2];

    return 0;
}